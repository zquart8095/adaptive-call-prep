langgraph>=0.2.0
pydantic>=2.6
python-dotenv>=1.0
anthropic>=0.40.0

# Optional — only imported by observability/tracing.py when
# LANGSMITH_API_KEY + LANGSMITH_TRACING=true are both set
langsmith>=0.10.0

pytest>=8.0
